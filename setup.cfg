[metadata]
name = graph-library
version = 1.0.1

[options]
zip_safe = False
include_package_data = True
packages = find:
python_requires = >=3.9

[options.packages.find]
where = .
exclude =
    tests*
    docs*
    examples*
